import React from 'react';
import MicroAppContainer from '@/components/MicroApp/MicroAppContainer';

const Vue3MicroApp: React.FC = () => {
  return (
    <MicroAppContainer
      name="vue3-micro-app"
      containerId="vue3-micro-app-container"
    />
  );
};

export default Vue3MicroApp;
